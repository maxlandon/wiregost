package profile

// StartRuntimeControl - Initializes all infrastructure for managing the implant runtime performances, debugging and profiling.
func StartRuntimeControl() {

        // Once all is set, start blocking loop (implant main() must not exit)
        for {

                // We should have some sort of circuit-breakers in case performances reach abnormal levels

        }
}
