package https

// StartListener - Starts an HTTP(S) listener, waiting for incoming ghost implant connections.
// This listener can either use HTTP or HTTPS, it depends on the configuration passed as parameter.
func StartListener(conf *Config) (server *Server, err error) {
	return
}
