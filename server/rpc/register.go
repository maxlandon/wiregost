package rpc

// RegisterHandlers - Based on which target operating system, registers all RPC handlers.
// This function works in a way similar to `NewGhost()` in the `ghosts` package: it only
// registers the appropriate "interfaces".
func RegisterHandlers() {

}
