package compiler

// Compile - Compiles a ghost implant with the current settings
type Compile struct {
}

// Execute - Run
func (c *Compile) Execute(args []string) (err error) {

	// Here we gather all settings gathered in the various
	// compiler files of this package, for transports, routing,
	// security and target settings.

	// We form a request object

	// Send it

	return
}
