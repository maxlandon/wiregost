package util

// AutoWrap - Wraps a text in proportion to the current terminal width.
func AutoWrap(text string) (wrapped string) {
	return
}

// Wrap - Wraps a text given a specified width
func Wrap(text string, width int) (wrapped string) {
	return
}
